env_name=${1:-profiling}

source "$( dirname "${BASH_SOURCE[0]}" )/environment"
if [ -z ${CONDA_PATH_BACKUP+x} ]; then export CONDA_PATH_BACKUP=""; fi
if [ -z ${PS1+x} ]; then export PS1=""; fi
source "$PROJECT_ENV/bin/activate" ${env_name}
